<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.majorcascanueces.psa">
    <uses-permission android:name="android.permission.INTERNET"/>
    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@drawable/psa_icon"
        android:label="@string/app_name"
        android:roundIcon="@drawable/psa_icon"
        android:supportsRtl="true"
        android:theme="@style/Theme.PSA"
        tools:targetApi="31">

        <meta-data
                android:name="com.google.android.geo.API_KEY"
                android:value="AIzaSyByxgXuw6unuyvjRaMY_808hUt1qlP31H0"/>

        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".ui.activities.AuthActivity">
        </activity>

        <activity android:name=".ui.activities.AppActivity"
            android:exported="true"
            android:theme="@style/Theme.PSA.NoActionBar">
        </activity>

    </application>

</manifest>